version: 2

sphinx:
    builder: html
    configuration: doc/source/conf.py

python:
    version: 3.7
    install:
        - requirements: doc/requirements.txt 
        - method: pip
          path: .
          extra_requirements:
              - docs
    system_packages: true
